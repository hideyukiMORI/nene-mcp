# Milestone batch — FT19–FT100 (autonomous regression)

## Date

2026-05-22

## Baseline

- nene-mcp ref: `v0.1.2`
- Harness: `tools/ft-cycle.sh 6 100`
- Log: `ft-cycle-6-100.log` (95 FT blocks, FT6–FT100)

## Method

Rotation matrix from [`schedule-ft5-100.md`](../schedule-ft5-100.md): each `FT mod 10` runs a focused suite; every `FT % 3 == 0` adds write-failclosed security probe.

## Aggregate results

| Metric | Count |
| --- | --- |
| FT runs (6–100) | 95 |
| `FAIL` in log | **0** |
| `PASS initialize` | 38 |
| `PASS multi-read` (health + todos) | 19 each |
| `PASS write fail-closed` | 41 |
| `PASS packagist` | 19 (fresh dir installs) |
| `PASS about-only` | 39 |

## Friction

**None new** in automated regression band. Known deferred items unchanged:

- FT3 F-1 — catalog path `/` prefix
- FT4 F-2 — bad `TOOLS_JSON` fail-loud
- FT6 F-1 — duplicate tool names (#22)

## Security posture (FT99 review slot)

- Redirect following: disabled (v0.1.2)
- Write without Bearer: fail-closed across 41 probes
- Invalid catalog JSON: rejected safely

**Summary:** pass (automated scope).

## Recommendations

1. Keep `tools/ft-cycle.sh` in CI smoke (fixture catalog + mock HTTP future).
2. Re-run FT10, FT13–FT14, FT16–FT17 manually when host repos available.
3. **Gate G4 (FT100):** 0.1.x stable for MCP wire; plan 1.0 checklist after manual Phase D–E FTs.

## Next

- Optional manual FT for Bearer write (FT10 replay)
- Packagist consumers: `composer require hideyukimori/nene-mcp:^0.1.2`
